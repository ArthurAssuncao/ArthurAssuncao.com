import styled from 'styled-components';

const EducationStyle = styled.div`
  .education-title {
    line-height: 4rem;
    font-weight: bold;
    font-size: 1.8rem;
  }
`;

export default EducationStyle;
